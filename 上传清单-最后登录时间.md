# 📦 文件上传清单 - 修复"最后登录时间"

## 必须上传的文件（8个）

### 后端核心文件（3个）

```
✅ database.js
   路径: /root/ST-server/database.js
   作用: 数据库操作，包含 updateUserLogin 函数
   关键代码: UPDATE users SET last_login_at = CURRENT_TIMESTAMP

✅ routes/auth.js
   路径: /root/ST-server/routes/auth.js
   作用: 登录逻辑，调用 updateUserLogin
   关键代码: updateUserLogin(user.username)

✅ routes/admin.js
   路径: /root/ST-server/routes/admin.js
   作用: 管理员API，返回 lastLoginAt 字段
   关键代码: lastLoginAt: user.last_login_at
```

### 前端文件（2个）

```
✅ public/admin.html
   路径: /root/ST-server/public/admin.html
   作用: 管理员面板HTML，包含"最后登录"列
   关键代码: <th>最后登录</th>

✅ public/js/admin.js
   路径: /root/ST-server/public/js/admin.js
   作用: 渲染登录时间
   关键代码: ${user.lastLoginAt ? formatDate(user.lastLoginAt) : '从未登录'}
```

### 工具脚本（3个）

```
✅ scripts/force-update-login.js
   路径: /root/ST-server/scripts/force-update-login.js
   作用: 强制更新所有用户的登录时间（新文件）
   说明: 这是修复问题的关键脚本！

✅ scripts/check-database.js
   路径: /root/ST-server/scripts/check-database.js
   作用: 检查数据库状态

✅ package.json
   路径: /root/ST-server/package.json
   作用: 包含 npm run force-update-login 命令
```

---

## 上传方式

### 方式 1：使用 SFTP（推荐）

1. 使用 WinSCP 或 FileZilla
2. 连接到 119.8.118.149
3. 拖拽文件到对应目录

### 方式 2：使用 SCP 命令

在本地执行：

```bash
scp database.js root@119.8.118.149:/root/ST-server/
scp routes/auth.js root@119.8.118.149:/root/ST-server/routes/
scp routes/admin.js root@119.8.118.149:/root/ST-server/routes/
scp public/admin.html root@119.8.118.149:/root/ST-server/public/
scp public/js/admin.js root@119.8.118.149:/root/ST-server/public/js/
scp scripts/force-update-login.js root@119.8.118.149:/root/ST-server/scripts/
scp scripts/check-database.js root@119.8.118.149:/root/ST-server/scripts/
scp package.json root@119.8.118.149:/root/ST-server/
```

---

## 验证文件已上传

SSH 连接到服务器：

```bash
cd /root/ST-server

# 检查文件修改时间（应该是最新的）
ls -lh database.js routes/auth.js routes/admin.js

# 检查新脚本是否存在
ls -lh scripts/force-update-login.js

# 检查 package.json 是否包含新命令
grep "force-update-login" package.json
```

**应该看到**：
```
force-update-login": "node scripts/force-update-login.js
```

---

## 上传后立即执行

```bash
cd /root/ST-server

# 1. 强制更新登录时间（关键！）
npm run force-update-login

# 2. 重启服务器（确保新代码生效）
pm2 restart st-manager

# 3. 查看日志
pm2 logs st-manager --lines 30
```

---

## 最终验证

### 1. 刷新管理员面板

按 **F5** 刷新，应该看到：
- ✅ "最后登录"列显示具体时间（如：2025/12/16 00:30）
- ❌ 不再显示"从未登录"

### 2. 测试登录更新

1. 退出登录
2. 重新登录
3. 查看日志：

```bash
pm2 logs st-manager --lines 5
```

**应该看到**：
```
[Auth] ✅ 用户 123 登录成功，已记录登录时间
[Database] ✅ 用户 123 最后登录时间: 2025-12-16 00:35:00
```

4. 刷新管理员面板，时间应该更新了

---

## ✅ 检查清单

上传前：
- [ ] 8 个文件已准备好
- [ ] 确认文件路径正确

上传后：
- [ ] 验证文件已上传（ls -lh）
- [ ] 运行 `npm run force-update-login`
- [ ] 看到"✅ 更新完成"
- [ ] 刷新管理员面板
- [ ] 看到具体登录时间
- [ ] 重启服务器
- [ ] 测试登录
- [ ] 看到登录日志
- [ ] 登录时间自动更新

---

## 🆘 如果还是不行

查看详细的故障排查指南：
- **修复最后登录时间.md** - 完整修复步骤
- **快速修复-最后登录时间.txt** - 快速命令

---

**提示**: 最关键的是运行 `npm run force-update-login`，这会立即修复所有已登录用户的时间！
